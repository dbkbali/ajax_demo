$(document).ready(function() {

  // 1) add action.rb - '/notes' for json route
  // 2) $.ajax to load notes on document ready
  //    debugger - to explore response
  // 3) dataType: 'json' - JSON.parse(response)
  // 4) Appending data -
  //    shortform '<section></section>' - '<section/>'
  // 5) Amend ajax to shortform $.get() && $.getJSON



  // 1) $.ajax for posting form to server
  // 2) shortform $.post() command
  //




  // Deferreds/Promises
  //
  // 1) Get loading indicator working
  // 2) Alert if server error
  //


});
